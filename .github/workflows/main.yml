name: Main
on:
  push:
jobs:
  build:
    name: 'Go Build'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'
      - name: "Install libheif"
        run: "chmod +x ./.github/workflows/build-libheif.sh && ./.github/workflows/build-libheif.sh"
      - run: './build.sh' # verify the thing compiles
  static:
    name: 'Go Static'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'
      - name: "Install libheif"
        run: "chmod +x ./.github/workflows/build-libheif.sh && ./.github/workflows/build-libheif.sh"
      - name: "Prepare: compile assets"
        run: "GOBIN=$PWD/bin go install -v ./cmd/utilities/compile_assets"
      - name: "Run: compile assets"
        run: "$PWD/bin/compile_assets"
      - name: "Prepare: staticcheck"
        run: 'go install honnef.co/go/tools/cmd/staticcheck@latest'
      - name: "Prepare: govulncheck"
        run: 'go install golang.org/x/vuln/cmd/govulncheck@latest'
      - run: 'go vet ./cmd/...'
      - run: 'staticcheck ./cmd/...'
      - name: "Run: govulncheck"
        run: '$(go env GOPATH)/bin/govulncheck ./cmd/...'
  test:
    name: 'Go Test'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'
      - name: "Install libheif"
        run: "chmod +x ./.github/workflows/build-libheif.sh && ./.github/workflows/build-libheif.sh"
      - name: "Prepare: compile assets"
        run: "GOBIN=$PWD/bin go install -v ./cmd/utilities/compile_assets"
      - name: "Run: compile assets"
        run: "$PWD/bin/compile_assets"
      - name: "Run: URL preview package tests"
        run: "go test ./url_previewing/... ./pipelines/_steps/url_preview ./pipelines/pipeline_preview"
      - name: "Run: thumbnail package tests"
        run: "go test ./thumbnailing/... ./pipelines/_steps/thumbnails ./pipelines/pipeline_thumbnail"
      - name: "Run: tests"
        run: "go test -c -v ./test && ./test.test '-test.v' -test.parallel 1" # cheat and work around working directory issues
        timeout-minutes: 30
  runtime-images:
    name: 'Runtime Images'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: "Build: slim runtime"
        run: 'docker build --target runtime -t mmr-runtime-slim:test .'
      - name: "Check: slim runtime tools"
        run: 'docker run --rm mmr-runtime-slim:test sh -lc "command -v rsvg-convert && command -v djxl && ! command -v ffmpeg"'
      - name: "Build: full runtime"
        run: 'docker build --target runtime-full -t mmr-runtime-full:test .'
      - name: "Check: full runtime tools"
        run: 'docker run --rm mmr-runtime-full:test sh -lc "command -v rsvg-convert && command -v djxl && command -v ffmpeg"'
